# Google Cloud Configuration
GCP_PROJECT_ID=your-gcp-project-id
CLOUD_RUN_REGION=europe-west1
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Vertex AI Configuration
VERTEX_AI_LOCATION=europe-west1
GEMINI_MODEL=gemini-1.5-pro-002

# BigQuery Configuration
BIGQUERY_DATASET=bnb_autohealer
BIGQUERY_TABLE=metrics

# MCP Server Configuration (Local)
MCP_SERVER_PORT=8081  # Use 8081 locally to avoid conflict with Java service on 8080
MCP_SERVER_HOST=0.0.0.0

# Anomaly Detection Thresholds
LATENCY_THRESHOLD_MS=500
KAFKA_LAG_THRESHOLD=5000
ERROR_RATE_THRESHOLD=0.05

# Auto-Healing Configuration
MIN_INSTANCES=1
MAX_INSTANCES=10
SCALE_UP_FACTOR=2
SCALE_DOWN_COOLDOWN_MINUTES=10

